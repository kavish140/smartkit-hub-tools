[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[headers]
  for = "/*"
  [headers.values]
    # Prevent caching during rapid development; adjust for production as needed
    Cache-Control = "no-cache"
